# Screenshots

This folder should contain screenshots of the RetinaGuard-AI web interface.

Recommended screenshot names:

- web_home.png
- prediction_result.png
- lesion_overlay.png
- safety_triage.png

Suggested capture list:

1. Home/upload page.
2. Prediction result with DR grade and confidence.
3. Lesion localization overlay with boxes.
4. Safety triage output and clinical report panel.

After adding screenshots, commit them with:

git add docs/screenshots
git commit -m "Add web interface screenshots"
git push origin main
